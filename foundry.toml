[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.28"
via_ir = true
optimizer = true 
optimizer_runs = 0 #!!!W set to 5000 after unit test review (this for deployment to reduce execution costs -20% (but +15% deployment costs))

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options


[dependencies]
openzeppelin-contracts = "5.0.2"