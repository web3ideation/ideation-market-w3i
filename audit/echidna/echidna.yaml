# audit/echidna/echidna.yaml
testLimit: 50000     # number of sequences to try
seqLen: 100          # max calls per sequence
shrinkLimit: 200     # (optional) shrinking effort when a failure is found
